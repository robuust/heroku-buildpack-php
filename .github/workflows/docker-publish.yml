name: Docker

on:
  push:
    # Publish `robuust` branch to Container Registry
    branches: 'pdo_sqlsrv'

jobs:
  # Push image to GitHub Packages.
  # See also https://docs.docker.com/docker-hub/builds/
  push:
    runs-on: ubuntu-latest-arm64
    if: github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU        
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx        
        uses: docker/setup-buildx-action@v3

      - name: Log into GitHub Container Registry
        run: echo "${{ secrets.CR_PAT }}" | docker login https://ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build & push image to GitHub Container Registry
        run: docker buildx build --pull --platform=linux/arm64,linux/amd64 --tag ghcr.io/robuust/heroku-php-build-heroku-24 --file $(pwd)/support/build/docker/heroku-24.Dockerfile --push .
  php84:
    # Ensure push job passes before pushing image.
    needs: push

    runs-on: ubuntu-latest-arm64
    if: github.event_name == 'push'

    steps:
      - name: Set up QEMU        
        uses: docker/setup-qemu-action@v3

      - name: Build & deploy php 8.4
        run: docker run --rm -e AWS_ACCESS_KEY_ID="${{ secrets.AWS_ACCESS_KEY_ID }}" -e AWS_SECRET_ACCESS_KEY="${{ secrets.AWS_SECRET_ACCESS_KEY }}" ghcr.io/robuust/heroku-php-build-heroku-24 deploy.sh php-8.4.20 --overwrite

      - name: Build & deploy ext-pdo_sqlsrv for php 8.4
        run: docker run --platform=linux/arm64 --rm -e AWS_ACCESS_KEY_ID="${{ secrets.AWS_ACCESS_KEY_ID }}" -e AWS_SECRET_ACCESS_KEY="${{ secrets.AWS_SECRET_ACCESS_KEY }}" ghcr.io/robuust/heroku-php-build-heroku-24 deploy.sh extensions/no-debug-non-zts-20240924/pdo_sqlsrv-5.13.1 --overwrite
  php84_x86:
    needs: php84

    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
      - name: Build & deploy ext-pdo_sqlsrv for php 8.4 (x86)
        run: docker run --rm -e AWS_ACCESS_KEY_ID="${{ secrets.AWS_ACCESS_KEY_ID }}" -e AWS_SECRET_ACCESS_KEY="${{ secrets.AWS_SECRET_ACCESS_KEY }}" ghcr.io/robuust/heroku-php-build-heroku-24 deploy.sh extensions/no-debug-non-zts-20240924/pdo_sqlsrv-5.13.1 --name=extensions/no-debug-non-zts-20240924/pdo_sqlsrv-5.13.1-x86 --overwrite
  php85:
    # Ensure push job passes before pushing image.
    needs: push

    runs-on: ubuntu-latest-arm64
    if: github.event_name == 'push'

    steps:
      - name: Set up QEMU        
        uses: docker/setup-qemu-action@v3

      - name: Build & deploy php 8.5
        run: docker run --rm -e AWS_ACCESS_KEY_ID="${{ secrets.AWS_ACCESS_KEY_ID }}" -e AWS_SECRET_ACCESS_KEY="${{ secrets.AWS_SECRET_ACCESS_KEY }}" ghcr.io/robuust/heroku-php-build-heroku-24 deploy.sh php-8.5.5 --overwrite

      - name: Build & deploy ext-pdo_sqlsrv for php 8.5
        run: docker run --platform=linux/arm64 --rm -e AWS_ACCESS_KEY_ID="${{ secrets.AWS_ACCESS_KEY_ID }}" -e AWS_SECRET_ACCESS_KEY="${{ secrets.AWS_SECRET_ACCESS_KEY }}" ghcr.io/robuust/heroku-php-build-heroku-24 deploy.sh extensions/no-debug-non-zts-20250925/pdo_sqlsrv-5.13.1 --overwrite
  php85_x86:
    needs: php85

    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
      - name: Build & deploy ext-pdo_sqlsrv for php 8.5 (x86)
        run: docker run --rm -e AWS_ACCESS_KEY_ID="${{ secrets.AWS_ACCESS_KEY_ID }}" -e AWS_SECRET_ACCESS_KEY="${{ secrets.AWS_SECRET_ACCESS_KEY }}" ghcr.io/robuust/heroku-php-build-heroku-24 deploy.sh extensions/no-debug-non-zts-20250925/pdo_sqlsrv-5.13.1 --name=extensions/no-debug-non-zts-20250925/pdo_sqlsrv-5.13.1-x86 --overwrite
